import React from "react";
// import Counter from "./hooks/useReducer()/Counter";
import Form from "./hooks/useReducer()/Form";


export default function App() {
    return ( <
        >

        <
        Form / >
        <
        />
    );
}